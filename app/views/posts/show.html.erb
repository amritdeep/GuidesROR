=========================================================
<br>
<h1>
	<strong><%= @post.title %></strong>
</h1><br>
by <b><%= @post.user.username %></b>

<p><%= @post.context %></p>

<%= render '/comments/comment', :locals => @comment %>
<%= render '/comments/form', :collection => @comments, :as => :comment %>


<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back to Post', posts_path %> | 
<%= link_to 'View All', posts_path %>