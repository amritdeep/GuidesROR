<h2>Add a Comment:</h2>
<%= form_for([@post, @post.comments.build]) do |f| %>

	<div class="field">
		<%= f.label :body %><br>
		<%= f.text_area :body, :rows => 4 %>
	</div>

	<div class="action">
		<%= f.submit "Add a Comment", :remote => true %>
	</div>
<% end %>	
